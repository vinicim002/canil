# Banco
spring.datasource.url=jdbc:postgresql://localhost:5433/canil_db
spring.datasource.username=canil_user
spring.datasource.password=canil_pass
spring.datasource.driver-class-name=org.postgresql.Driver

# JPA
spring.jpa.hibernate.ddl-auto=update
spring.jpa.database-platform=org.hibernate.dialect.PostgreSQLDialect
spring.jpa.show-sql=true
spring.jpa.properties.hibernate.format_sql=true

# Flyway
spring.flyway.enabled=false

# JWT
jwt.secret=canil-alto-bela-vista-secret-key-dev-256-bits-minimo
jwt.expiration=28800000
jwt.refresh-expiration=604800000

# Cloudinary
cloudinary.cloud-name=dfrt0mwue
cloudinary.api-key=653737229867927
cloudinary.api-secret=OdrULJjyUyqSFTjpUVf2iIJldvg

app.cors.allowed-origins=http://localhost:5173,http://localhost:3000
app.cors.max-age=3600

# E-mail (Gmail SMTP)
# Senha de app do Google: use sem espaços (ex.: abcdabcdabcdabcd)
# O remetente (from) deve ser o mesmo e-mail autenticado no Gmail
app.mail.enabled=${MAIL_ENABLED:true}
app.mail.from=${MAIL_FROM:viniciusvilanova7@gmail.com}
app.mail.admin=${MAIL_ADMIN:viniciusvilanova7@gmail.com}
app.mail.site-name=Canil Alto da Bela Vista
app.mail.site-url=http://localhost:5173

# Agendamento público de visitas (sem login)
app.visita.site-url=http://localhost:5173
app.visita.management-path=/agendamento
app.visita.hora-inicio=09:00
app.visita.hora-fim=17:00
app.visita.duracao-minutos=60
app.visita.antecedencia-minima-horas=24
app.visita.dias-antecedencia-maxima=60
app.visita.dias-permitidos=1,2,3,4,5,6
app.visita.notificar-email=true

# n8n — automações WhatsApp (Evolution API no n8n)
# O workflow no n8n PRECISA estar ATIVO (toggle verde) senão dá 404 no webhook.
app.n8n.enabled=${N8N_ENABLED:true}
app.n8n.webhook-secret=${N8N_WEBHOOK_SECRET:canil-n8n-dev-secret}
app.n8n.webhook-url-visita=${N8N_WEBHOOK_URL_VISITA:http://localhost:5678/webhook/canil-visita-eventos}
app.n8n.connect-timeout-ms=5000
app.n8n.read-timeout-ms=10000

spring.mail.host=${MAIL_HOST:smtp.gmail.com}
spring.mail.port=${MAIL_PORT:587}
spring.mail.username=${MAIL_USERNAME:viniciusvilanova7@gmail.com}
spring.mail.password=${MAIL_PASSWORD:xosyfciekrcppqhj}
spring.mail.properties.mail.smtp.auth=true
spring.mail.properties.mail.smtp.starttls.enable=true
spring.mail.properties.mail.smtp.starttls.required=true