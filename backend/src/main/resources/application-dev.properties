# Banco
spring.datasource.url=jdbc:postgresql://localhost:5433/canil_db
spring.datasource.username=canil_user
spring.datasource.password=canil_pass
spring.datasource.driver-class-name=org.postgresql.Driver

# JPA
spring.jpa.hibernate.ddl-auto=update
spring.jpa.database-platform=org.hibernate.dialect.PostgreSQLDialect
spring.jpa.show-sql=true
spring.jpa.properties.hibernate.format_sql=true

# Flyway
spring.flyway.enabled=false

# JWT
jwt.secret=canil-alto-bela-vista-secret-key-dev-256-bits-minimo
jwt.expiration=28800000
jwt.refresh-expiration=604800000

# Cloudinary
cloudinary.cloud-name=dfrt0mwue
cloudinary.api-key=653737229867927
cloudinary.api-secret=OdrULJjyUyqSFTjpUVf2iIJldvg

app.cors.allowed-origins=http://localhost:5173,http://localhost:3000
app.cors.max-age=3600

# E-mail (Gmail SMTP)
# Senha de app do Google: use sem espaços (ex.: abcdabcdabcdabcd)
# O remetente (from) deve ser o mesmo e-mail autenticado no Gmail
app.mail.enabled=${MAIL_ENABLED:true}
app.mail.from=${MAIL_FROM:viniciusvilanova7@gmail.com}
app.mail.admin=${MAIL_ADMIN:viniciusvilanova7@gmail.com}
app.mail.site-name=Canil Alto da Bela Vista
app.mail.site-url=http://localhost:5173

spring.mail.host=${MAIL_HOST:smtp.gmail.com}
spring.mail.port=${MAIL_PORT:587}
spring.mail.username=${MAIL_USERNAME:viniciusvilanova7@gmail.com}
spring.mail.password=${MAIL_PASSWORD:onfubbyickdxgyvs}
spring.mail.properties.mail.smtp.auth=true
spring.mail.properties.mail.smtp.starttls.enable=true
spring.mail.properties.mail.smtp.starttls.required=true